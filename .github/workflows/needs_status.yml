name: GITHUB Needs status
on:
  workflow_call:
  workflow_dispatch:

jobs:
  first:
    runs-on: ubuntu-latest
    steps:        
      - run: exit 1
  second: 
    if: always()
    needs: first
    runs-on: ubuntu-latest
    steps:
     - run: echo ${{needs.result}}
      
